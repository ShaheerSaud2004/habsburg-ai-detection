# Python caches
__pycache__/
*.pyc

# ML virtualenv (torch + transformers; only gpt2_perplexity.py needs it)
.venv-ml/

# PDF build artifacts (regenerated by build_pdf.py; PAPER.pdf itself is kept)
paper_build.md
header_pdf.tex

# GPT-2 perplexity cache (regenerated by gpt2_perplexity.py)
corpora/gpt2_ppl.tsv

# Large downloaded corpus (regenerated by fetch_corpora.py)
corpora/HC3_all.jsonl

# Generated outputs (regenerated by experiments.py / evaluate.py / crossmodel.py / visualizations.py)
experiment_results.json
evaluation_results.json
crossmodel_results.json
lomo_results.json
lomo_gpt2_results.json
collapse_results.json
collapse_gpt2_results.json
figure*.png

# The derived text corpora are small; keep them so results are reproducible
# without re-downloading. Comment the next two lines out if you'd rather not commit them.
# corpora/hc3_human.txt
# corpora/hc3_llm.txt
lomo_multiseed_results.json
corpora/gltr.tsv
detector_comparison.json
collapse_scaling_results.json
collapse_modern_results.json

# private / local-only
.claude/
site/.vercel/
STARTUP_BRIEF.md
LINKEDIN_POST.md
SUBMISSION_KIT.md
